// import Link from "next/link";

const ApplyForm = () => {
  return (
    <div className=" w-full h-full bg-primary-100 text-primary-300 hover:text-primary-500 text-sm md:text-base lg:text-lg rounded-md p-2 md:p-4 lg:p-6 flex flex-col justify-center items-center">
      <span className="font-bold">프론트엔드 지원 마감</span>

      <span>지원해주셔서 감사합니다!</span>
    </div>
  );
};

export default ApplyForm;
